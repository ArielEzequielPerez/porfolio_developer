version: '3.8'

services:
  porfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
